const log = console.log;

const funcA = (num) => num%2 == 0? num: num+1;
const power = (a, b) => a**b;

log(funcA(125));
log(power(2, 10));

// let num = 13;
// let arr = [];

// new Array(10).fill(0).forEach((e, i) => {})

// console.log(num%2 == 0? funcA(num): funcB(num));

// arr.push(num%2 == 0? true: false);

// arr[100] = arr[0];

// console.log(arr, arr.length);

