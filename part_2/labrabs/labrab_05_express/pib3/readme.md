# se-25

## labrab-03  

### tasks  

- написать express-приложение  
- считать `data.json`  

#### 01

- вывести json в браузер  
- при переходе на главную страницу `/`
  - выводить как есть (как в файле)  
- при переходе по пути `/order`  
  - вывести отсортироваными по возрастанию id  
  
---  

#### 02

- пути `/users` и `/` - должны выводить данные как есть  
- пользователь может выбрать по какому полю сортировать (`id`, `title`), в каком направлении сортировать и сколько первых вывести - `count` (например, `count` = 3)  
- если пользователь ввёл неправильно (не число или такого id не существует), то вывести всех как есть  

---  
