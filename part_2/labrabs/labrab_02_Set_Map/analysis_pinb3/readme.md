
- вот такой подход возможен:  

```js
  let isAscending = true;
  for (let j = 1; j < nums.length; j++) {
    if (nums[j] <= nums[j - 1]) {
      isAscending = false;
      break;
    }
  }
```

но мы стремимся перейти к функциональному стилю:  

```js
  const isAscending = nums
    .toString() === nums.toSorted((a,b) => a-b).toString()
    // .toSorted((a,b) => a-b).every((val, ind) => val === nums[ind])
    // .every((num, ind) => ind === 0 || num > nums[ind-1])
```

> отличия `.sort()` и `.toSorted()`  

---  

и не надо грузить в свои репозитории на GitHub папку `/node_modules`  

---  

```js
const memUsage = process.memoryUsage();
console.log(memUsage);
// Вывод (значения в байтах):
// {
//     rss: 4935680,         // весь объём совсемтно с движком V8
//     heapTotal: 1826816,   // объем обектов V8
//     heapUsed: 650472,     // объем объектов JS
//     external: 49879,      // объекты C++ не из кучи, но управляемые V8
//     arrayBuffers: 9386    // часть external под ArrayBuffer
// }
// ArrayBuffer - **область** памяти, последовательность байтов
// используется через посредников 
//   - типизированные массивы Uint8Array, Int32Array, Float64Array
//   - или чтения в бинарном виде (Сокеты, Файлы, Ассемблер)
```

---  

### Примитивные типы данных  

1. **number** — целые числа и числа с плавающей точкой  
2. **string** — строки (текст)  
3. **boolean** — логические значения (`true` или `false`)  
4. **undefined** — означает, что значение не присвоено  
5. **null** — означает «ничего» (задаётся разработчиком)  
6. **symbol** — уникальный идентификатор  
7. **bigint** — для очень больших целых чисел  

- хранят только одно значение  
- неизменяемы  
- передаются `по значению`  
- сравниваются непосредственно  

### Составные типы данных  

**Object** — базовый составной тип, от него происходят остальные:  

- **Array** (массив)  
- **Function** (функция)  
- **Date** (дата)  
- **RegExp** (регулярное выражение)  
- **Map**, **Set**, **WeakMap**, **WeakSet** (коллекции)  

- коллекции данных  
- сложные структуры  
- передаются `по ссылке`  
- НЕ сравниваются непосредственно  

---  

```js
null == undefined;  // true  (нестрогое сравнение, равны)
null === undefined; // false (строгое сравнение, разные типы)  

Number(undefined);  // NaN (Не число)
Number(null);       // 0
```

> undefined - значение еще не установлено  
> null - хоть это и примитив, но по логике работы это ссылка без указания на объект  
> только преднамеренно так можно сделать  
> typeof null возвращает "object" (это баг первых версий языка)  

---  

**`undefined`** используется для проверки, существует ли свойство или было ли присвоено значение  
**`null`** используется для "обнуления" переменной или явного указания, что объекта нет (ПОКА нет, но потом планируется туда записать новый объект)  

---  
